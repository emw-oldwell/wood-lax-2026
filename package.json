{
  "name": "wood-lax-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Wood Family Lacrosse — Summer 2026 schedule + lodging dashboard",
  "scripts": {
    "build": "node build.js",
    "preview": "node build.js && python3 -m http.server 8000 || python -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
